# Required credentials — fill in before running prod-setup.sh
# prod-setup.sh reads these and writes them to ./secrets/ as individual files
# for bind-mount into containers (no plaintext env vars in production).

OPENAI_API_KEY=
JUPYTERHUB_URL=
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
FLASK_SECRET_KEY=

# Optional: set to "true" to enable the /testDB debug endpoint (admin-only)
# ENABLE_DEBUG_ENDPOINTS=false
